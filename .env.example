# Database Configuration
# Get these from Supabase Dashboard > Project Settings > Database
# Use Connection Pooling URL for DATABASE_URL (port 6543)
# Use Direct Connection URL for DIRECT_URL (port 5432)
DATABASE_URL="postgresql://postgres.USERNAME:PASSWORD@aws-1-us-east-1.pooler.supabase.com:6543/postgres"
DIRECT_URL="postgresql://postgres:PASSWORD@db.USERNAME.supabase.co:5432/postgres"

# Clerk Authentication
# Get these from Clerk Dashboard > API Keys
# Publishable key (safe to expose in frontend)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Secret key (keep private, server-side only)
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Application URLs
# Frontend URL (where your Next.js app runs)
FRONTEND_URL=http://localhost:3000
# Backend API Server URL (where your Express server runs)
NEXT_PUBLIC_SERVER_URL=http://localhost:4000
# Sidecar Service URL (for file operations, etc.)
NEXT_PUBLIC_SIDECAR_URL=http://localhost:4001

# Supabase Configuration (optional, if using Supabase features beyond database)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here

# Environment
NODE_ENV=development

# Server Ports (optional, defaults shown)
# SERVER_PORT=4000
# SIDECAR_PORT=4001
